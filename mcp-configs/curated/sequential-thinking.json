{
  "_description": "Sequential Thinking MCP server — structured chain-of-thought reasoning for complex multi-step problems",
  "_install": "No auth required.",
  "mcpServers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    }
  }
}
