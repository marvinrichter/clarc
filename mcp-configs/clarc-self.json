{
  "mcpServers": {
    "clarc": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/index.js"],
      "env": {
        "CLAUDE_WORKDIR": "${workspaceFolder}"
      },
      "description": "clarc MCP server — 8 tools: get_instinct_status, get_session_context, get_skill_index, get_project_context, skill_search, agent_describe, rule_check, workflow_suggest"
    }
  }
}
