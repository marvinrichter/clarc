# Learning Flywheel

**Status:** ✅ Done | **Date:** 2026-03-09 | **PR:** #4

**Goal:** Instinct lifecycle with quality gate: observe → evolve → promote → export.

## Key Outcomes
- `learn-eval` command — quality-gated instinct extraction from sessions
- `/evolve` command — clusters accumulated instincts into patterns
- `instinct-outcome` command — records outcome (good/bad) for quality signal
- `continuous-learning-v2` skill updated with full lifecycle documentation
